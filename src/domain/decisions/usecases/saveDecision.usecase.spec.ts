import { saveDecisionUsecase } from './saveDecision.usecase'

describe('saveDecision', () => {
  const usecase = new saveDecisionUsecase()

  it('returns decision object when decision is saved successfully', () => {
    // GIVEN
    const decision = {}
    // WHEN

    // THEN
  })
})
